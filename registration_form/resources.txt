*** Settings ***

Library           SeleniumLibrary

*** Variables ***
${SERVER}       localhost:5000
${browser}      chrome
${url}      https://demoqa.com/automation-practice-form


*** Keywords ***
User is in Student Registration Form
    Open Browser    ${url}    ${browser}
    Maximize Browser Window
    Student Registration Form must be ready

Student Registration Form must be ready
    Element Should Contain      titleHeader     STUDENT REGISTRATION FORM

User fill the field "${field_Name}" with "${field_value}"
    Input Text    "${field_Name}"   "${field_value}"


